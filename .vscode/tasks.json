{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "command": "cargo build --verbose -p nsga_rs --features plotting",
      "label": "Cargo build (dev)",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
